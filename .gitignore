__pycache__/
*.pyc

# 运行时数据库 / 缓存（自动生成）
data/cache/*.db
data/db/*.db
data/*.db

# 自动生成的报告
data/reports/*.md
data/reports/*.json

# 数据源配额账本 / 优化器状态（自动生成，含时间戳）
data/cache/asharehub_quota.json*
data/cache/.last_optimize_*
data/cache/.last_*

# 优化器输出权重
data/weights/

# 运行时日志
logs/

# 零时缓存文件
data/cache/codes_cache.json

# 工作区 / 归档
docs/

# WorkBuddy 自动化运行时状态（记忆/工件，不属仓库内容）
.workbuddy/
stock-picker.code-workspace
.claude/
.env

# 临时测试文件 — 绝对不要提交
_verify*.py
*_tmp.py
tmp_*.py
*.bak

# 工作文档
audit_data_consistency.md

# 归档周报
reports/backtest_weekly.md

# 2026-09-19 补：备份库/分析缓存/临时对账文件不入库
data/backup/*.db
data/backup/*.json
data/cache/*.pkl
data/_bf_*.json

# 2026-09-21 补：门禁/诊断/推送运行时日志（每日重生成，不入库）
data/cache/*.log
