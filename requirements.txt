# 核心依赖
mootdx>=0.10.0            # 通达信 TCP 行情（K线+财务快照，永不封IP）
requests>=2.28.0          # HTTP API 直连（腾讯行情/同花顺热点/东财）
pandas>=2.0.0             # 数据处理与 DataFrame 操作

# 辅助依赖
numpy>=1.24.0             # 因子计算
scikit-learn>=1.2.0       # Ridge 回归权重优化
akshare>=1.14.0           # A 股代码列表 + 大单资金流数据
cachetools>=5.3.0         # 实时行情内存缓存 (TTLCache)
pyyaml>=6.0               # 配置文件加载 (config.yml)
asharehub>=0.6.0          # 北向资金持仓数据（需免费 API Key：https://asharehub.com/console/register）
