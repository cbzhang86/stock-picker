# 2026-09-05 工作日志

## 数据管线全链路测试（10:41-11:10）✅

- 新增 `scripts/test_data_pipeline.py`：29 项测试覆盖 data_engine 全部接口（配额管理/ASHareHub 逻辑/真实连通/免费行情/东财同花顺 akshare），支持 `--only` 选择性重跑
- 结果：27 PASS / 1 FAIL / 1 WARN，报告见 `reports/数据管线测试报告_2026-09-05.md`
- **配额纪律**：ASHareHub 全天仅 1 次真实调用（C1: financial_indicators(600519) roe=26.48 ✓ key 有效）；本地账本已同步 `{"date":"2026-09-05","used":1}`
- **发现的真实缺陷**：
  1. B10 — `asharehub_prefetch.db` 存在 NaN 字面量 JSON（8/29 批次），读回进评分路径；修复方向：`_write_asharehub_prefetch` 写前 NaN→None + 清理存量 78 行
  2. E9 WARN — `_get_ths_fund_flow` 茅台 36.34 亿主力净流入口径可疑，列位/单位待人工核对
- **连接器覆盖结论**：ASHareHub 5 个数据面中 4 个可被 TDX/问财/东财/同花顺/mootdx 完全替代；唯一缺口个股级北向（权重已归 0，影响可控）→ ASHareHub 可降级为"增强源"而非硬依赖
- 教训：测试中 fresh_engine 曾共享临时账本文件导致配额污染（A2/B1-B5/C1 假 FAIL），已修复为 per-instance 临时路径

## 完成项

### Task 3（TDX/问财数据通路）✅
- `core/fundamental_provider.py` 与 `core/event_provider.py` 已存在并验证
- `core/factor_library.py` 已挂载 `attach_tdx_signals` 静态方法 + `compute_all_factors` 新增两因子
- `core/scoring_model.py` 已加 `valuation_fundamental` / `event_catalyst` 中性检测
- `strategies/short_term.py` 已在 backtest 路径用 `as_of=backtest_date` 传 reference_date（防止 look-ahead）
- `scripts/prefetch_tdx.py` 已可写盘
- **本次新增**：用 `prefetch_tdx.py` 写入 5 只样本（茅台/宁德/比亚迪/生益/海康）的基本面 + 5 条事件样本
- 端到端验证：`attach_tdx_signals` → `compute_all_factors` → `score_stock` 通路跑通，5 只样本新因子 raw_score 区间 [42.8, 72.0]，方向合理
- 事件自动打标命中（业绩预增/回购/分红 → 3 正；问询/减持 → 2 负），且 5 天衰减系数生效（不是满 impact）

### Task 4（专家 Ensemble 第二意见）✅
- 新建 `core/expert_ensemble.py`：
  - `ExpertScorer`：5 维独立评分（fundamental 25 / technical 25 / capital 20 / valuation 15 / event 15）
  - `ExpertEnsemble.fuse`：与主模型融合，三档 confidence：
    - `high`（|Δ|≤10）：consensus +1.5 bonus，仓位系数 1.00
    - `medium`（10<|Δ|≤25）：降权温和，仓位系数 0.85
    - `conflict`（|Δ|>25）：打分扣 8 分 + ⚠️ 告警，仓位系数 0.50
- 在 `strategies/short_term.py` run 流程里嵌入 ensemble 步骤（在 `PortfolioOptimizer.allocate` 之前）
- `core/portfolio_optimizer.py._scoring_weight` 改成 `effective_score = 0.6 × score + 0.4 × ensemble_score × weight_factor`，让冲突档股票被显著降仓
- 集成测试：4 只样本（茅台/宁德/海康/生益）全部得到 ensemble_score + confidence，海康（追高+泡沫+减持）正确归为 medium 档 → 仓位被压低

### 文件变更清单（本次新增/修改）
- **NEW** `core/expert_ensemble.py` — 5 维专家评分 + 融合器
- **MOD** `strategies/short_term.py` — 集成 ensemble（在板块/龙虎榜补算之后、portfolio_optimizer 之前）
- **MOD** `core/portfolio_optimizer.py` — `_scoring_weight` 支持 ensemble_score 加权
- **NEW** `data/cache/fundamentals_seed.json` — 5 只样本基本面种子数据
- **NEW** `data/cache/events_seed.json` — 5 条样本事件种子数据
- **NEW** `data/reports/tdx_smoke_test_2026-09-05.json` — Task 3 通路 smoke test 报告

## 待办 / 注意事项

1. **v1.json 暂未加入 valuation_fundamental / event_catalyst** — 当前权重 0，等 OOS IC 验证有效后再 calibrate_weights.py 提升权重
2. **TDX 数据真实覆盖需要批量拉数** — 当前仅 5 只样本；生产用需对全市场做一轮 `tdx_indicator_select` 预取
3. **Ensemble 在 production 路径生效需 short_term.run 跑通** — 已通过独立集成测试验证 4 档路径，但未跑 live run（环境无行情）
4. **未触发 'low' 档** — 当前测试样本 Δ 都在 high/medium/conflict 范围内；'low' 档（|Δ|介于 medium 和 conflict 之外）当前未触发，需要更细致阈值再设计

## 项目全面审查（多维分级）✅

- 通读全部核心代码（oos_validator/technical_scorer/calibrate_weights/backtest_engine 全段/long_term/scoring_model 中段），报告输出至 `reports/项目全面审查报告_2026-09-05.md`
- **结论：无 P0；P1×3 / P2×9 / P3×11**，三个 P1 全部指向"权重决策证据链断裂"：
  1. **P1-1** legacy `run_kline_factor_backtest` 池化 IC + 因子口径与生产不一致（momentum 用原始涨幅非 RPS、technical 简化版、volume_price log 映射）→ 双 IC 方法论矛盾，legacy 结论应作废
  2. **P1-2** hot_theme 占 50% 权重但回测中恒为缺省 50 分且不在 neutral 让渡名单 → 策略级回测失真；OOS IC+0.047/ICIR0.88 存在候选池自选择偏差
  3. **P1-3** capital_flow 权重 0.3036 但主口径 OOS IC 仅 +0.0045（t=0.11）且覆盖 10.5% → 30% 权重压在噪声上（ASHareHub 配额约束覆盖）
- **已实锤的新发现**：`_calc_monthly_returns` 的 `zip(records, trade_returns_t1)` 在 skip 记录时错位（最小探针复现）；volume_price `base_vp*0.7+tail_bonus` 无尾盘数据封顶 59.5 vs 有数据 ~89.5（数据可得性被当 alpha）
- **其他要点**：_assess_market 涨停阈值 9.5% 忽略 20cm/30cm 板；逐笔 Sharpe ×√252 口径失真；calibrate_weights 报告"当前权重"读 config.yml 与生效的 v1.json 脱节；oos_validator 文档说不填 50 但代码 fillna(50) 矛盾；validator technical 量能用 vma5 生产用 vma20
- **路线图**：第一批纯修复（zip/NaN/死代码/权重源/文档）→ 第二批恢复回测可信度（hot_theme neutral 化/legacy kfactor 降级/快照 O(D×N) 优化）→ 第三批数据积累后重校准（capital_flow 扩覆盖/因子相关性矩阵/自选择对照）
- **正面评价**：oos_validator + calibrate_weights 的防过拟合链路（时间切分/walk-forward/三口径悲观/λ 收缩/上限裁剪）是项目最强部分，方向正确，无需推倒

## 审查报告优化落地（12 项，12:00-12:30）✅

按用户要求逐项落地审查报告的修复，每项验证通过后再进行下一项：

**第一批（纯修复）**
1. P2-1 `_calc_monthly_returns` 改传 trade_details，消除 zip 错位（真实方法调用验证）
2. P2-7 `data_engine.sanitize_nan()` 写入端 NaN/Inf→None + `allow_nan=False` 兜底；清理存量 58 行（tech_factors 17 + financial 41），复检 0 残留
3. P3-5 technical_scorer：score() 入口拦截坏价（≤0/NaN→中性50）；删 avg5 死代码；avg20/current_v 缺失→"量能数据不足"9 分；support truthiness 修复（5 用例回归）
4. P2-8 oos_validator 文档改为与代码一致（hot_theme/dragon_tiger fillna(50) 是有意对齐实盘基线）
5. P3-8 calibrate_weights 当前权重源改 v1.json 优先（实际运行验证日志"当前权重来源: data/weights/v1.json"）

**第二批（恢复回测/实盘可信度）**
6. P1-2① scoring_model neutral 名单加入 hot_theme（`'is_hot_stock' not in stock_data and not blocks and not concept_names`），回测场景 technical 有效权重 0.089→0.315；实盘"查过非热点"不受影响
7. P2-3 volume_price 无尾盘数据不再 ×0.7（无数据 80 分 vs 旧 56 分；有数据路径与旧公式一致）
8. P2-4 _assess_market 涨停判定板块化（10/20/30cm 三档阈值 9.5/19.6/29.4），合成行情 2涨1跌→6涨2跌
9. P2-6 score_stock 硬失败统一 0 分 + `risk_blocked` 标记；批量/单票路径得分一致性验证通过
10. P1-1 kfactor 降级：run_backtest.py 打印 DEPRECATED 警告 + 方法 docstring 标记指向 --mode oos
11. P1-3 v1.json 权重调整：capital_flow 0.3036→0.15，technical→0.16，volume_price→0.12，momentum→0.04，dragon_tiger→0.03（备份 `v1.backup_20260905_review.json`；总和 1.0 验证）
12. P2-2 快照构建索引化（{date_str: 行号}），40 股×303 日逐行等价，7.40s→2.77s（2.7x）；验证脚本 `_verify_snapshot_perf.py`

**环境注意**：项目脚本必须用 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311\python.exe`（managed 3.13 无 numpy/pandas）

**遗留（第三批，需数据积累）**：热点历史回放表、hot_theme 自选择对照实验、capital_flow 免费源扩覆盖后重跑 OOS、因子相关性矩阵、ensemble 置信阈值校准

## 稳定盈利能力对标评估（14:54-15:25）✅

- 报告：`reports/模型稳定盈利能力评估_2026-09-05.md`，四维度（回测绩效/因子有效性/稳健性/横向对标）
- **新增实证**（全部来自项目真实数据）：
  1. 从 backtest_trades 逐笔算 t 值：run22 t=+1.58 / run23 t=+1.74，95% CI 均含 0 → **统计不显著**；需 ~150-200 笔才能过 2
  2. 中位数≈0、偏度 +0.4~0.7、盈亏比 1.56（盈利笔均 +4.97% / 亏损 -3.19%）→ 靠右尾盈利的结构
  3. **月度一致性 run23 仅 3/8 个月正收益**（1月胜率17% 亏损 / 6月胜率72% 大赚）→ 题材市波段盈利而非稳定盈利，与 hot_theme 50% 权重直接对应
  4. hot_theme 隔夜结构：hold1d/overnight 显著正（+0.047/+0.085），intraday 反向 → 题材热度 100% 在隔夜跳空，与尾盘买入设计匹配但依赖热点生态延续
  5. K 线三因子 walk-forward 第一折翻正后转负 → 环境依赖实锤
  6. **幸存者偏差实锤**：代码列表 = ak.stock_info_a_code_name() 当前上市名单（4588 只）回填 2025-06 历史已退市股缺失 → 回测收益系统性高估
- **最终判定：未达标**——证据不足以认定稳定盈利；但具备"正期望结构的题材短线策略雏形"（框架 A-，绩效 C+，因子 C，稳健性 C-）
- 风险 Top5：hot_theme 集中+自选择 / t<2 / 幸存者偏差+口径错位+基准不可靠（全方向高估）/ 单一窗口被 23 次迭代用尽 / 右尾依赖
- 改进 ROI 排序：时间积累（60天OOS+200笔）→ 修复口径三角 → 去幸存者偏差 → hot_theme 对照实验 → 成本压力测试（滑点 0.2/0.3%）→ 市场状态仓位开关 → K 线反向因子对照
- 补跑：修复后代码 2026-07-01~09-04 回测仍在 K 线预热（4588 只补抓耗时长），结果将落盘 `reports/backtest_postfix_20260905.json`

## 技能/连接器盘点 + 剩余优化落地（15:36-16:00）✅

**连接器实测**：mx-ds-mcp/tdx-connector/westock-mcp 在会话中无直接 MCP 工具（ToolSearch 验证），经 Skill 中转（relay）使用；tdx-connector 有 10 个工具（行情/K线/选股/研报/公告/宏观）。

**新增优化落地（4 项，全部验证）**：
1. P3-9 基准修复：`_calc_benchmark_return` 失败→None + `benchmark_available` 字段贯穿 BacktestResult（三场景单测）
2. 市场状态仓位开关：弱市 `position_scale=0.5`（总仓 50% 留现金），`_apply_position_scale` 静态方法（3 用例单测）；极差市维持 skip 全停
3. 成本压力测试（精确变换法）：run23 100 笔滑点 0.1/0.2/0.3% → 均T1 +0.89/+0.69/+0.49%，t=1.74/1.35/**0.96** → 0.3% 滑点下期望为正但与 0 无法区分，执行滑点必须压在 0.2% 内
4. 分组对照实验（`_verify_factor_experiments.py`，890 只/48 日面板）：
   - **momentum 五分位完美单调**，Q5-Q1=-0.772%/天 → 建议加"动量 top20% 硬过滤"而非仅降权
   - **hot_theme 流动性匹配后溢价仍 +1.945%/天（t=5.26，71% 桶正）** → 自选择只解释 ~19%，热度独立 alpha 成立 → 上调 hot_theme 可信度，风险点①降级为"有证据的集中"

**评估报告已更新**：附录二（两组实验）写入 `reports/模型稳定盈利能力评估_2026-09-05.md`

## 项目定位确认 + 胜率对齐落地（16:13-16:35）✅

**用户明确定位**：不对接实盘、不自动下单，仅尾盘候选筛选，买入由用户手动执行——westock 模拟交易建议保留为纸面跟踪用途。

**胜率对齐（评估改进7 具体化）**：
- 新增 `_verify_winrate_segments.py`：热点×动量分位交叉胜率（890 只×48 日）
- 关键发现：热点组胜率 ~58% vs 非热点 ~45%；**非热点·动量Q5 是毒药段（胜率 40.3%/日均-1.09%，n=7370）**；热点·Q5 仍 55.9%/+1.44%（豁免不误伤）；热点·Q4 反而是热点内最弱段（48.4%，小样本待观察）
- 落地：`short_term._apply_momentum_filter`（非热点 rps_20>=80 剔除，热点豁免，config `momentum_filter_rps_max: 80` 可关），已写入 config.yml buy 段
- 预期：面板级 +1.1pp；策略级（推荐以热点为主，过滤主要保护漏网的非热点高动量票）待回测确认

**顺带修复**：P3-2 is_hot_stock 布尔化；P3-3 RPS 横截面 O(N²)→O(N)（short_term+long_term，等价性验证）；P3-11 kfactor 死代码删除

**回归测试**：7 项全部通过（config 新参数/score_stock 风控与 neutral/v1.json 权重/technical_scorer 坏价/volume_price/backtest 新签名/long_term RPS）——今日全部改动互不破坏

**注意**：后台基线回测 R1dmC2 启动早于动量过滤，其结果=「修复后、过滤前」基线；过滤后需再跑一次对比（等 K 线缓存预热完成，约 1-2 小时）

## 架构对标分析（16:35-16:50）✅

- 报告：`reports/架构对标分析_2026-09-05.md`，对标 qlib/WorldQuant/Barra/私募通行做法
- 架构梳理补齐：feedback/data_collector（factor_daily 采集）、optimizer（Ridge 权重优化，自动写入已停用）、tracker（predictions.db T+1/5/20 追踪）
- 核心判断：验证方法论与工程纪律已达主流水准；差距在数据广度（PIT 宇宙/全市场因子覆盖/raw 值）与样本时间——均可"投入时间购买"；ML（LightGBM ranker）排在最后，60+ 天面板前不上是对的
- ROI 总表 11 项：#1 每周 OOS 节律 + 冻结期纪律 → #2 组合层风控（同板块≤2 + 连亏降仓）→ #3 全市场因子采集 → #4 factor_corr/ic_decay → #5 回测 hold1d 口径+分层滑点 → #6 raw 值列 → #7 风格因子批量池 → #8 evaluate_all 门禁 → #9 退市宇宙 → #10 面板 Ridge → #11 LightGBM

## 架构对标方案落地（16:38-17:00）✅

**已落地并验证（#2/#4/#6/#8/#5/#1）**：
1. **#2 组合层风控**：`_apply_board_diversification`（同板块≤2，风格/地域板块不约束）+ `_get_losing_streak_scale`（连亏 N 日→仓位×0.5，查询 predictions.db，样本不足/异常返回 1.0）；config 新增 max_per_board/losing_streak_days/losing_streak_scale。单测：板块约束 7 场景 + 连亏 SQL 验证
2. **#4 因子诊断三件套**（oos_validator 新增）：factor_corr（按对计数修 NaN 污染）/ ic_decay（新增 T+3/T+5 收益口径）/ neutralized_ic（桶内 rank——**教训：跨桶 rank 会稀释二分信号，须桶内 rank 才与同桶对照同构**）。实测：hot_theme 与其他因子相关仅 0.02-0.07（独立 alpha）；**热度衰减 T+1 +0.058 → T+3 +0.029 → T+5 +0.010，半衰期 2-3 天**；中性化后 hot_theme 保留 +0.036，momentum 反向与流动性无关
3. **#6 factor_raw 表**：data_collector 增 raw 值落库（volume_ratio/rps_20/turnover/pct_chg/amount/raw_return_20），缺数据写 NULL 不写 0
4. **#8 evaluate_all 门禁**：9 项检查（编译/回归/权重/口径一致性）+ --full OOS 诊断；一次 FAIL（DEFAULT_WEIGHTS 导入错误）修复后 9/9 通过
5. **#5 回测成交口径**：fill_convention='open_t1'（默认，历史可比）|'close_t0'（尾盘买对齐实盘）+ slippage_tiers 分层滑点（按当日成交额）。合成 K 线 4 用例精确验证（含 BacktestResult 四舍五入到 2 位的断言修正）
6. **#1 定时节律**：创建 2 个自动化——每周一 08:30 evaluate_all --full 因子健康诊断；每月 1 日 calibrate_weights 建议报告（绝不自动 --apply）

**重大发现（遗留）**：后台基线回测（run_id=24，2026-07-01~09-04）**仅 1 笔交易**——42/45 个交易日被评"极差市"skip。根因：回测模式下市场评估的强势股数恒为 0（hot_df=None）、北向恒为 0，两维度系统性压分 → 弱势窗口几乎全停。**回测-实盘评估环境不对等**，非代码错误。后续决策项：回测模式给这两个维度中性默认值，或接受保守行为。动量过滤在回测日志 0 次触发也与此相关（skip 在评分之前发生）。

**未落地（需数据/时间）**：#3 全市场资金流回填（网络密集）、#7 风格因子批量池（依赖 #4 工具已就绪，下次会话可做）、#9 退市宇宙（baostock 数据工程）、#10 面板 Ridge（依赖 #6 raw 数据积累）、#11 LightGBM（60+ 天面板后）

## 设计决策记录

- **Ensemble 不修改 model_score** — 作为 `expert_adjusted_score` 附加，避免主模型被双重解释
- **confidence 4 档而非 3 档** — 预留 'low' 给中度噪声（避免 high→medium→conflict 三档断崖）
- **冲突判定 |Δ|>25** — 5 维专家评分理论上应与 7 因子主模型大致同向，|Δ|>25 几乎一定是某一方严重偏离（数据异常/因子失效），需要人工审视而非系统兜底

## 代码体检 + 尾盘定时推送（17:20-18:00）✅

- evaluate_all --full：10/10 全过（hot_theme IC=+0.0580）
- 冒烟测试 eod_stock_picker --mode short：7m11s 全流程跑通（周六以 09-04 数据运行；弱市 52.5 分无达标标的属正常风控）；测试后已恢复 3 个 DB 备份清除周六噪声
- **修复 2 个真实 bug**：
  1. `strategies/short_term.py` 缺 `import os` → 连亏降仓风控静默失效（`name 'os' is not defined`），冒烟测试日志抓到
  2. `eod_stock_picker.py --out` 死参数 → 已实现（另存简报到指定路径）
- **ASHareHub 401 根因**：key 本身有效（SDK 复测 57 行），是 agent 非交互 shell 不读 .bashrc 导致进程环境变量为空；已写入 Windows 用户环境变量（OK:52），提示词加 export 兜底
- 微信推送方案：WorkBuddy 自动化原生「推送到小程序」+「微信助理集成」扫码绑定，零第三方密钥；automation_update 工具本会话不可用，用户在自动化界面创建
- 交付：`docs/自动化任务_尾盘选股推送.md`（可直接粘贴的自动化配置+提示词）

## 2026-09-05（晚）策略设计全面审查

- 交付：`策略设计全面审查报告_2026-09-05.md`（全量代码走读 + OOS 证据核对，8 维度 × 行业对照）
- **P0 发现（意图≠行为）**：
  1. ExpertEnsemble 空转——src_for_ensemble 从瘦 rec 取字段全为 None → 专家分恒 50，min_score=75 下全部误标 conflict（修法：`rec.get('_src', rec)`）
  2. 涨停封死过滤死代码——`limit_up_amount` 无任何数据源填充，实盘/回测均不生效
  3. 权重三源失同步——config.yml(0.3036) ≠ v1.json(0.15) ≠ 文档旧值；实际生效 v1.json
  4. 卖出规则（止盈/止损/T+3 时间止损）从未进回测模拟
  5. 印花税 0.05%/过户费缺失；逐笔收益当逐日算 Sharpe → 4.36 为虚高口径
- 关键口径漂移：volume_ratio 三套定义（腾讯分钟折算 / 回测20日均量 / OOS面板5日均量）；回测市场评估 hot_score 恒 0
- 前视隐患：FundamentalProvider 无 as_of，回测挂今日基本面到历史日期（当前权重 0 无实害）
- 结论：先修 P0 再重跑基线，之后才谈 P1（扩样本一年重验 + hot_theme 复合公式拆分验证 + DSR/PBO）

## 全代码库缺陷审查收尾（11:30-12:00）

- 终验 18/18 通过（乱序索引/时间戳、量纲、原生标量、json.dumps、边界）
- **新发现并修复 2 个高影响缺陷**（单位考证：akshare 官方文档 + 同花顺实页 51.57元×14662股=75.61万元 吻合）：
  - **D7** vwap=Σ成交金额/Σ成交量 = 万元/股 = 真实均价/1e4 → price_position≈+9999 恒成立，"强势收尾"+6 分对所有有大单数据的票恒触发 → 改量加权均价 Σ(价格×量)/Σ量
  - **D8** 大单回退源成交额（万元）未换算，ASHareHub/THS 源均为元 → main_fund_accumulated 混源差 1e4 倍，skip_asharehub 主路径横截面百分位失真 → big_deal net ×1e4；get_tail_end_stats total_net/tail_net 同步 ×1e4
- 回归：evaluate_all --full 10/10 PASS（hot_theme IC +0.0570，与修复前同水平）
- 交付：reports/代码缺陷审查修复报告_2026-09-05.md（D1-D8 完整原因/影响/修复 + 非缺陷记录 + 遗留事项）
- 遗留：①历史 capital_flow 快照万元口径断层（2026-09-05 起=元）；②capital_flow 权重可在 D7/D8 修复后重估（calibrate_weights.py）；③临时脚本已清理
- 教训：同一消息内并行 Edit 同一文件会互相覆盖（先写者被后写快照冲掉），重文件编辑必须串行

## 审查报告改进落实收尾（下午-21:00，Task #4/#5 完成）

- **P3 全部完成**：P3-Q drift_monitor.py 接线进 eod_stock_picker.py main()（步骤 1.5，非阻塞，写 data/reports/drift_latest.json）；P3-S scripts/capacity_check.py（ADV/参与率/分档滑点查表与回测同口径/sqrt 冲击 impact_const×σ_daily×√participation/参与率≤10% 倒推容量，--offline 直读 kline_cache.db，冒烟：9 票参与率<0.1%、双边 37-47bp）；P3-P tests/test_improvements_20260905.py（66 用例覆盖 P0-E/P2-K/N/P0-B/P1-G/P2-K/P0-D/P2-N/P1-F/P3-S/P3-Q）
- **测试修复 3 处**（均为用例设计失误非产品 bug）：①乱序 tiers 查表期望值写反（5000 归 1e12 档=0.008 才对）；②stop_price_abs=9.5 时 low=9.7 未击穿、止盈先触发是对的，改 abs=9.75（同时验证 abs 止损下双触发仍保守取止损）；③winsorize 只压幅度不改序，断言改为 e>d 且 e<99（无截断时 z≈65→100.00）
- **Task #5 回归验证**：py_compile 15 文件 + import smoke + verify.check_weights_consistency=OK（loaded==v1.json/caps OK/config=历史草稿）+ unittest 66/66 OK (0.24s)
- **SKILL.md 因子架构表更新**（P0-A 文档遗留项）：改为 v1.json 生效值（hot_theme 50/technical 16/capital_flow 15/volume_price 12/momentum 4/dragon_tiger 3/north_flow 0），注明唯一事实源、各因子 OOS IC、新因子待验证、risk 涨停代理规则
- **交付**：变更摘要_2026-09-05审查报告落实.md（P0-A~E/P1-F,G,H,I,J/P2-K,L,N/P3-P,Q,R,S 全量改动↔报告依据↔预期效果映射 + 验证结果 + 接口兼容性 + 遗留事项）
- 遗留提醒：standardize 默认 false（启用须重跑 OOS+calibrate）；新因子等快照 ≥60 交易日；git 工作区含两轮改动未提交，建议用户确认后整体提交一次

## 全量验证（23:24-00:50，用户要求完整验证）

- **静态**：py_compile 57 文件 0 失败；import smoke 34 模块全过（排除 evaluate_all/test_data_pipeline 两个自执行脚本）
- **发现并修复 3 个真实缺陷**：
  1. **evaluate_all.py 引用断裂**——P1-J 重命名 `_calc_benchmark_return`→`_calc_benchmark_returns` 后门禁没同步，导致门禁 8/9 FAIL；修复为多基准断言（全基准返回 + 失败记 None），门禁恢复 9/9
  2. **show_status 权重展示残留**（P0-A 漏网）——`--status` 显示 config 历史草稿权重而非 v1.json 生效值；修复为 ScoringModel().get_weights('short') + None 收益显示"待回填"不再误标 ❌
  3. **run_backtest.py 无 socket 超时**——周六凌晨非交易时段跑回测挂死 30 分钟（CPU 仅 7 分钟，Not Responding，阻塞在无超时网络调用）；修复：加 `socket.setdefaulttimeout(15)`（与 eod_stock_picker 同款）。**教训：所有会触网的入口脚本都该有全局 socket 超时**
- **单元/一致性**：unittest 66/66 OK；check_weights_consistency OK
- **业务端到端**：--status 正常（36 条记录，胜率 41.7%/avg +0.10%）；漂移监控真实数据返回 INSUFFICIENT（33 结果日 <40，按设计不误报）；run_backtest --mode short 2026-06-15~06-27 真实回测启动（首跑大量"K线缓存残缺"fall through 重抓 mootdx 逐票补缓存，耗时较长属正常，跑完后缓存补全下次回测加速）
- 交付：docs/项目介绍与运行指南.md（定位/架构/因子权重表/环境/全部命令与预期结果/风控机制/验证状态/避坑/下一步）
- **真实回测端到端**：run#25（06-15~06-27）与 run#26（04-01~04-30）全链路 EXIT:0。两轮均 0 交易——run#25 因极差市（评分 9.8 按设计停推）；run#26 为**修复后评分变保守的实证**：4 月强市日子（市场评分 73.5/81）下"没有评分达标标的"（momentum 25%→4%、涨停代理过滤生效、回测口径 hot_theme 中性化，min_score=75 无达标）。旧 run#22 同区间 52 笔不可复现 = 挤水分，非退步。回测/实盘口径对齐需 hot_theme 历史快照接入回测（数据工程遗留项）
- 回测首次跑新区间耗时长（逐票校验缓存+fall through 重抓 mootdx，run#26 约 98 分钟），跑完后 kline_cache 大幅补全，重跑同区间会快数倍；建议未来把"缓存预热"做成独立命令（scripts/prefetch 已有部分能力）
- mootdx 个别票返回异常日期（0-00-00）→ 已被捕获记 warning 不阻断，健壮性 OK
- 全量验证结论：57 编译 + 34 导入 + 门禁 9/9 + 单测 66/66 + 权重 OK + 业务工具全通 + 真实回测链路跑通，**无阻断性 bug**
