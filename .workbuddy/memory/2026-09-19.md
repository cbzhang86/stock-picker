# 2026-09-19 工作日志

## 因子扩容 + 权重论证（OOS 实证，未改业务代码）

用户指令：扩充因子并经论证设置权重；过往数据也能验证，不必等累计数据。

**做法**：确认 `kline_cache` 有 2024-01 起 OHLCV+amount（299.9 万行 / 647 天 / 5225 只），
直接用历史 K 线做 OOS，不依赖落库预测数据。

**产出**：`docs/因子扩容与权重论证_20260918.md`（完整论证报告）

**关键结论**
1. 新增 `liq_dev`（缩量偏离）/ `vol_dev`（波动偏离），方向=偏离常态越低分越高
2. 证伪原始 `liquidity`：84% 是小市值效应；偏离成分 ICIR 反超（0.535 > 0.418）且与规模正交
3. 生产 `factor_library.py:688` liquidity 方向与实证相反，需修正
4. 权重判据 = Top-N 尾部收益，不是 IC（等权方案 IC 最高却几乎不赚钱）
5. hot_stocks 无前视偏差（当日已涨 10.5%），但 86.4% 接近涨停不可买

## 用户决策与方案定稿（2026-09-19 上午）

1. **口径确认：尾盘买入** → `ret_hold1d` 正确，09-18 基于 `ret_intraday` 的"基线最优"结论不适用
2. **不归零** → 定稿**方案 G（保守混合）**：hot .55 / liq_dev .14 / rev .10 / vol_dev .07 / vol .06 / mom .02 / tech .02 / vp .02 / dt .01 / cf .01（Σ=1.00）
   - Top5 日均超额 +1.845%（t=21.4）vs 基线 +0.850%；仅比纯净版 E 低 0.031 pp（2025 年反超）
   - 压力测试：hot 源失效后 E/G 都退化到 ~0（G 略好 +0.04 pp）→ 0.08 噪声腿不构成真实兜底，真防线是 source_status 熔断告警
3. **多 AI 辩论机制**：项目内**没有**（grep 零命中）；出处 = `docs/外部参考_四仓库源码级分析_2026-09-17.md` 对 TradingAgents-CN 的架构分析。已有 `ExpertEnsemble` 是可测试等价物（分歧度→置信度→仓位）。**建议不引入运行时 LLM 辩论**（不可回测/成本延迟/形态不匹配/瓶颈不在此）；替代两步走：① 多视角分歧度指标（因子组独立排名→分歧度→回测验证→接 confidence 管道）；② LLM 事后复盘笔记（离线落库 ≥60 日再评估）

## 落地完成（2026-09-19 上午，全部验证通过）

**权重三层已写入方案 G**（Σ=1.00）：hot .55 / liq_dev .14 / rev .10 / vol_dev .07 / vol .06 / mom .02 / tech .02 / vp .02 / dt .01 / cf .01（north_flow/size/liquidity=0）
- `data/weights/v1.json` / `config.yml`（含完整论证注释）/ `core/scoring_model.py DEFAULT_WEIGHTS` 三层同步

**代码改动**
- `core/scoring_model.py rank_stocks`（volatility 块后）：`_liq_dev_raw`（log(amount_t)−60日中位数）→ 横截面百分位；`_vol_dev_raw`（vol20−60日中位数）→ 横截面百分位
- `core/factor_library.py`：`liq_dev`=(1−pct)×100（缩量高分）、`vol_dev`=(1−pct)×100（波动收敛高分），缺失→中性 50
- `core/oos_validator.py`：K_FACTORS 注册 + 面板计算（level60/vol_level60 滚动中位数分解）
- **liquidity 方向未翻转、权重保持 0**（与报告 6.2 原清单的偏差）：OOS 证据属 log(amount) 口径，生产 liquidity 是 turnover 乘积口径，不可套用；正确做法 = liq_dev 替代

**验证结果（三道全过）**
- 全量单元测试 **404 passed**（`python -m unittest discover -s tests -q`，无 pytest 环境）
- 门禁 `scripts/evaluate_all.py` **9 通过 / 0 失败**
- OOS 复验（正式面板）：liq_dev IC +0.0538/ICIR 0.404/t+5.64，vol_dev IC +0.0268/ICIR 0.224/t+3.13（与临时脚本同号同量级）

**寻优确认（落地前置条件，用户要求）**：训练(≤2025-08,355天)/持有(≥2025-09,245天)切分，500 组 Dirichlet + 坐标精修：训练期最优（hot .37/liq_dev .14/vol_dev .07/vp .05）持有期 +1.754% **反而低于 G 的 +1.768%**（过拟合特征）；500 随机解无一显著超越 G。**G 在持有期前沿，确认最优**。寻优解 liq_dev .14/vol_dev .07 与 G 完全一致（独立收敛佐证）。脚本 `scripts/_tmp_weight_opt.py`

**ExpertEnsemble 查证（用户问题 2）**：**已落地且生产每日运行**（short_term.py:414-465 接入、portfolio_optimizer effective_score=0.6×score+0.4×ensemble×wf、daily_job_20260917/18.log 有"ensemble 完成"记录）。**但实效存疑**：生产日志 expert≈48-50 vs model≈74-75 恒冲突——专家 5 维因基本面/估值/资金流缺失退化为中性 50，"失明专家"系统性压低高分票仓位。后续课题：按 confidence 分档回测次日收益，若冲突档不更差则改为"专家维度覆盖率≥阈值才参与融合"

**复盘知识闭环设计（用户问题 3，报告第九章）**：三关 = ①结构化落库（notes 表：thesis/verdict/missed_risk/key_factors/**prediction**/realized_outcome，LLM 只填 3 个字段，verdict 与 outcome 程序写入）；②预测-对照循环（每条笔记必须含次日方向预测，T+1 自动对账，周命中率≤50% 即熔断停该环节）；③月度蒸馏（bad 笔记 key_factors 聚类→候选规则→历史面板回测→审批生效→月度监控→退役，LLM 产生假设、回测决定采纳）。次日 prompt 注入 2-3 条相似历史案例（key_factors 标签匹配，无需向量库）。明确不做：笔记直接改分/自由文本总结/推送链路同步调 LLM

**踩坑（新）**
- config.yml 编辑时 old_string 结尾带 `\n` 而新串不带 → 把下一行并进注释行，`capital_flow: 0.01` 被吞（已修复）；编辑 YAML 时**绝不能让 old/new 串的尾换行不对称**
- 项目无 pytest，测试用 `python -m unittest discover -s tests -q`（404 个）

## 第三轮落地（2026-09-19 上午，用户三指令：采纳G/完善专家五维/落地复盘）

**① G 方案**：上轮已落地（权重三层 + liq_dev/vol_dev 因子 + 404 测试 + 门禁 9/0 + OOS 复验），本轮用户正式采纳。

**② 专家五维"失明"修复（`core/expert_ensemble.py`）**
- 根因：`score()` 5 维等权平均、缺失维一律 50 → 生产常只有 technical 有数据（0.25）→ expert 恒≈50 → 对 74-75 分票恒 Δ≈−25 conflict → −8 分 + 仓位 0.5
- 修复：①覆盖率门控 `MIN_EXPERT_COVERAGE=0.40`（=至少两维有数据），不足 → `confidence='low_coverage'`、`ensemble=model`、`abstained=True`、仓位系数 1.0；②已覆盖维重归一 `Σ(raw×w)/Σw`，缺失维不再稀释；③`confidence_to_weight_factor` 增 `low_coverage→1.00`，其余四档不变
- `ExpertVerdict` 新增 `coverage`/`abstained` 字段，breakdown 每维带 `covered`；short_term.py 日志加"弃权"计数
- 测试：新增 `tests/test_expert_coverage_gate_20260919.py`（8 例）；旧 `test_improvements_20260905.py` 公式测试改用 `_AllNeutralScorer` 桩（coverage=1.0），`test_expert_neutral_baseline` 改为断言弃权
- 遗留课题：按 confidence 分档回测次日收益，量化 conflict 档区分度（数据积累后）

**③ 复盘系统落地（`scripts/postmortem.py` + 自动化）**
- CLI 五命令：add / backfill（K线回填 T+1 收益与 verdict，毛收益口径）/ stats（命中率，≤50% 退出码 1 熔断）/ similar（key_factors 标签重合检索）/ candidates（bad 笔记标签频次→月度蒸馏候选）
- verdict 阈值：≥+0.5% good / ≤−0.5% bad / 其余 neutral；命中判定：up>0、down<0、flat |x|≤0.5
- LLM 只填 thesis/key_factors/prediction/missed_risk 四字段；verdict 与收益程序写入；UNIQUE(date,code) 防重；停推日不虚构
- **自动化已创建**：ID `52413995-57ab-46c9-aaec-20ee5995d794`，周一~五 19:35，周五加跑 stats；文档 `docs/自动化任务_尾盘选股推送.md` 第 8 节
- 测试：`tests/test_postmortem_20260919.py`（10 例，全过）

**验证**：unittest **422 passed** + 门禁 **9/0**

**踩坑**
- `ret_hold1d` 单位是百分数，按小数处理会让复利曲线爆炸（MDD −1e30%）
- 环境无 pyarrow/fastparquet，面板缓存改用 pickle
- 面板含价格错误离群值（max +350729%），需按 ±35% 裁剪

**未改动任何业务代码**，临时脚本仅 `_tmp_` 前缀。

## 第四轮：全项目深查（用户要求复盘漏检原因 + 全模块审查 + 达标评估）

报告：`docs/全项目深查报告_20260919.md`（未改任何代码，等审批）

**漏检根因（五条）**：①意见类组件无效果验收（只有接线层验收）；②测试固化缺陷（test_expert_neutral_baseline 把 expert=50 断言为正确）；③日志只查存在性不查模式（恒冲突 14 天无人质疑）；④跨模块数据流审查缺位（P0-B 依赖 _src 填充率无人验证）；⑤"缺失→中性50"语义从未清单化

**新发现（分级，均未修复待审批）**
- **P1-1** score_stock 中性让渡白名单未含 liq_dev/vol_dev/volatility/liquidity → 缺失时以"真实50"计入权重，与 OOS 的 NaN 剔除口径不一致（与专家失明同构）；影响次新股排序 + 回测快照模式（三腿合计权重 0.27）。修复≈10行：按 `_*_percentile is None` 判 is_neutral
- **P1-2** `_SOURCE_FACTOR_IMPACT` 未登记新因子（mootdx_kline 映射缺 liq_dev/vol_dev/volatility）→ K线源故障时简报漏算受影响权重 0.27，违反 09-16 契约#3。修复=2行映射+守卫测试（"非零权重因子必须出现在数据源映射中"）
- **P1-3** 动态门槛 65/70/75 基于旧权重尺度（hot 底座 29.4→38.5）→ 门槛语义漂移、弱市保护减弱、零推荐行为静默变化（与 09-14 门槛错配同类）。修复：观察 5-10 日记录 Top5 分数分布后重校
- **P2-1** calibrate_weights 按 IC 共识优化，与"IC≠尾部收益"实证冲突，月末 --apply 可能覆盖 G → 加警告
- **P2-2** briefing source_status 回退静默全绿 → 加 warning
- **P2-3** data_engine:2118 主题排序 isinstance(float) NaN 不设防（展示层）
- **排除项**：calibrate_weights 候选池是通用的（会自动纳入新因子，非缺陷）；除法点位均有守卫；静默 except 抽查均合理

**优先级**：周一开盘前修 P1-1+P1-2 → 本周观察 P1-3 → 月内 P2-1+confidence 分档回测 → 长期：缺失语义决策表/组件覆盖率日志/日志模式告警

## 第五轮：深查问题全部修复 + 六维验证通过（用户批准后执行）

**修复清单（6 项全部落地）**
| 编号 | 修复 | 文件 |
|---|---|---|
| P1-1 | 中性让渡白名单扩至百分位驱动因子（liq_dev/vol_dev/volatility/liquidity/size）；**momentum/reversal 有意不改**（data_engine:1272 无K线返回恒50而非None，改判定会动 0.12 权重动量腿，属未验证语义变更 → 记遗留） | core/scoring_model.py |
| P1-2 | mootdx_kline/baostock_kline 映射补齐新因子 + reversal_20d | reports/market_briefing.py |
| P1-3 | **不改门槛数值**，建立可校准通道 `scripts/recalibrate_thresholds.py`（--from-run-context 实测 / --oos-proxy 代理） | 新增脚本 |
| P1-4 | `sys.exit(main())` 透传退出码（原 `main()` 丢失 return 2 → 自动化误判成功） | scripts/calibrate_weights.py |
| P2-1 | `--apply` 增加 `--accept-ic-objective` 闸门（解析后 fail-fast） | scripts/calibrate_weights.py |
| P2-2 | source_status 回退分支加 warning | reports/market_briefing.py |
| P2-3 | 主题榜排序改 math.isfinite | core/data_engine.py |

**P1-3 修正结论（代理实测）**：分数分布**分化而非平移**——池内 60 分位 68.3→63.0、80 分位 69.5→66.0（旧 A → 新 G）；通过率代理：门槛 65/70 基本不变，**门槛 75（弱市）92.5%→97.5%（+5.0pp）**，即弱市"零推荐日"减少约 5pp。故不用单一偏移量改门槛，改为监控 15 个交易日实测再定。

**六维验证结果**：①单元/契约 **438 passed**（+16）②门禁 **9/0** ③集成（rank→score 混合K线长度）通过 ④**无副作用**：零让渡时评分严格等于 Σ(因子分×权重)（places=2）⑤行为：`--apply` 被拒退出码=2 且 v1.json md5 未变 ⑥量化代理见上

**守卫测试（防复发）** `tests/test_audit_fixes_20260919.py`（16 例）：百分位因子白名单守卫 / 数据源依赖表守卫（按族声明，不用"全部因子"哨兵）/ 退出码守卫

**踩坑**：测试里"数据完整"要连 capital_flow 的 main_fund 一起给，否则让渡逻辑会引入 0.01 权重再分配导致断言差 0.15；size 无 total_market_cap 会被让渡但权重 0 无影响 → 断言只看有权重因子

## 第六轮：代码冻结确认 + 项目全景导览

- 用户确认"暂时不动" → **代码冻结（稳定版 v-G）**，进入观察期
- 观察期三件事：①看下周一起新版权重实盘简报 ②攒复盘命中率与 run_context 门槛分布（15 交易日后跑 recalibrate_thresholds.py --from-run-context）③两个已知量化效应（弱市有推荐天数 +5pp；热点股集中在当日涨 5-9% 未涨停）
- 再动代码的触发条件：门槛重校有明确建议 / 复盘连续熔断 / 数据源长期失效 / 新因子方向（流程已固化）
- 产出导览文档：`docs/项目全景导览_20260919.md`（面向"从零理解"的完整介绍：架构图/因子权重表/三道防线/质量体系/自动化时间表/异常处理/术语词典）

## 每日调度自动化（14:51，周六非交易日）

- 非交易日分支：预取退出码 0，全程约 90s。ASHareHub 闸门 90/100 预检命中（连续第 3 天）→ 0 次新配额消耗，仅预取东财（blocks 30 / dragon_tiger 30）；本批 30 只（offset=126）tech/concepts/financial 全 miss，待配额恢复补抓。
- predictions.db 备份正常；估值快照累计 10,438 行 / 2 个交易日（≥60 交易日才启用 OOS 验证）。
- 按规则非交易日成功不推送微信。

## 第七轮：目录清理（用户指令）

- 已清：__pycache__ 4 个目录（122 个 .pyc，1.84MB，自动再生）；临时日志 6 个
- 已归档（不删，保证据链）：scripts/_tmp_*.py 5 个 → `archive/analysis_202609/scripts/`；data/reports/_* 2 个 + 根目录游离脚本 _verify_emotion_0917.py → `archive/analysis_202609/reports/`
- 保留：kline_cache.db 378MB（核心数据资产）、valuation_history.db 38.7MB、v1.backup_*.json 4 个（回滚保险）、data/backup 每日 predictions 备份、data/logs（30 天保留约定，总量仅 5.9MB）
- 待用户决定：_tmp_panel pkl 360MB（oos-proxy 依赖，建议门槛重校完成后删）；**git 提交建议**：今天 8 个代码文件改动未提交，建议冻结版打一次 commit
- 清理后根目录仅剩标准条目；清理后抽测 27 用例通过，总占用 796MB（不含 .git）

- **冻结提交完成**：commit `97af698`（52 文件 / +5755 / −80），工作区干净。提交前修正 gitignore 三处漏配：data/backup/*.db、data/cache/*.pkl（361MB 面板缓存）、data/_bf_*.json —— 均已从暂存区移出，文件保留在磁盘。工作日志/自动化元数据随本次首次入库

- **临时文件清理（用户逐项确认后执行）**：删除 data/_bf_*.json 2 个、0 字节空壳库 4 个（data/ 根下 predictions/backtest/backtest_store + data/db/factor_daily.db，均已核实无代码引用；活跃库 data/db/predictions.db 与 data/cache/factor_daily.db 完好）、实验日志 51 个（bt_/ablate_/oos_ 前缀）；tests/verify 16 个一次性脚本归档至 archive/analysis_202609/tests_verify/。清理提交 `d5e0f22`。残留：data/logs 中约 10 个 analysis_*/fix_* 前缀同类日志未被过滤器匹配（量小，保留）。⚠️ 本会话 bash 输出通道不稳（du/sort/PowerShell stdout 失效、计数器误报），文件操作一律用 Python 并以磁盘实况核验
