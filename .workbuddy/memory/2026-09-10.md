# 2026-09-10

## 14:50 每日统一调度（自动化 8ac61aa8）

- 交易日分支正常：退出码 0，耗时约 2 分 15 秒。
- 市场环境综合评分 26.2/100（极差市）→ 短线策略强制停推（涨 913/跌 4195，涨停 41/跌停 16，中位涨幅 -1.41%）；成交 15,840 亿，北向 +37.20 亿。
- 长线关注 5 只照常输出（平安银行 71.0 分居首）；权重优化报告照常生成、建议未生效等待审批（符合设计）。
- 已知告警：数据质量 1 行涨跌幅越界（|pct|>31%）；昨日 asharehub 三源 NameError 熔断今日未复现。
- 微信全文推送成功（1 条）。

## 定时任务模型设置排查（12:15-12:30）

- 需求：用户问「两个定时任务的大模型如何设置」，并希望改用 dsv4.1flash
- **automation_update 工具第 5 次不可用**（DeferExecuteTool 报 not available）→ 改为只读排查
- **自动化配置存储位置（重要发现）**：`~/.workbuddy/workbuddy.db` → 表 `automations`，关键列：
  `model_id`(模型) / `model_is_thinking`(思考开关) / `skills_json` / `push_to_wechat` / `permission_mode` / `rrule` / `scheduled_at` / `valid_from|until` / `cwds` / `deleted_at`(软删)
  另有 `automation_runs` / `automation_runtime_state` / `automation_delivery_outbox`
  **读法（安全）**：把 db+wal+shm 复制到临时目录再用 `file:...?mode=ro` 只读查询，绝不直连原库写
- **当前 6 条生效自动化的模型**（均 thinking=off）：
  - glm-5.3-flash ×4：每日14:50调度 / 周度健康检查 / 每周OOS诊断 / 每月权重校准
  - hy4-preview ×2：A股T+1结果回填（周一~五18:00）/ 11:30群聊素材推送（一次性）
  - 另 4 条为软删除（3 个旧「交易日选股」+1 个微信推送测试），界面看不到，无需处理
- 6 条提示词**都已包含 `send_wechat.py` 推送命令**（确认用户之前的粘贴已生效）
- 模型标识参考：应用内置模型表（cli/dist/codebuddy.js）含 `deepseek-v4-flash`（name "DeepSeek V4 Flash"，cost in/out $0.14/$0.28 per M）与 `deepseek-v4-flash-free`、`deepseek-v4-pro`；本项目会话横幅显示的 "Deepseek-V4.1-Flash" 即该 flash 档；`balanced-model` 为服务端别名
- 结论：模型需在「自动化」编辑界面逐条切换（我无法程序化改），建议直接选界面里的 dsv4.1flash

## 18:00 T+1 回填（自动化 2bc09d30）

- 成功 4 / 失败 0 / 滞留 18，微信推送成功（详细模式）。08-13 批 4 条补齐 T+20 出池，pending 30→26。
- K=18 口径自洽：16（09-09）−4（08-13 出池）+6（08-20 新入 >20 天窗口）=18，全部仅缺 T+20 未到期。
- 回填本身无静默失败；但 **predictions 最新批次仍为 09-07**，09-08/09-09/09-10 连续 3 日无新产出。
  - 09-10 可解释：14:50 调度因市场环境 26.2 分触发短线强制停推，未落库。
  - 09-08/09-09 同样缺失 → 需单独查 14:50 调度那两日是否也走了停推分支或未执行。
