# 2026-09-16 工作日志

## daily_job 调度 + technical_scorer None 崩溃修复

- 14:50 统一调度两次崩溃于 `core/technical_scorer.py::score_from_asharehub`：`tech.get('rsi_12', 50)` 默认值仅在键缺失时生效，API 返回 null 值字段时 `rsi12 + rsi24` TypeError。两次复现确认为持续性数据缺陷。
- 最小防御修复（无人值守默认决策，待审阅）：`score_from_asharehub` 内加 `_num()` helper，macd/macd_dif/macd_dea/rsi_12/rsi_24/cci 六字段 null → 按原缺省值兜底；正常数据评分语义零变化。
- 副作用：两次崩溃运行耗尽当日 ASHareHub 配额（100/天），第三次运行四源降级中性 → 当日评分系统性偏低，仅 1 只达标（600163 中闽能源 65.06）。
- 第三次运行成功，退出码 0。市场 86.0/100 强市，涨 3888/跌 1132，成交 18,383 亿，北向 +8.81 亿。
- 微信 ClawBot 全文推送成功（1 条）。
- 交付：data/reports/short_20260916.md、short_20260916_briefing.md。
- 待办：用户审阅 technical_scorer.py 修复。

## 当日异常全量审计（用户要求，交付审计报告）

交付：`docs/问题审计报告_当日异常_2026-09-16.md`（1 P0 + 3 P1 + 4 P2 + 3 项核实非缺陷）。

关键新增结论（超出崩溃本身）：
- **P0-1 根因链完整闭环**：上游壳记录（close_hfq 有值、macd=0、rsi/cci 为 null，缓存中占 18.3%/23 条）→ live `float(nan)` 产出 NaN 不报错 → 写缓存 `sanitize_nan` NaN→None（P2-7）→ **读缓存无逆变换** → 消费端 `get(k,default)` 对 null 无效 → TypeError。崩溃票锁定为初筛第 1 名 **001309 德明利**（09-13 周日预取、age 3d 命中缓存，故日志无其网络请求）。
- **P1-2 新发现（风控）**：`portfolio_optimizer._scoring_weight` 先截断 MAX_ALLOCATION=0.40 再 `pct/total*100` 归一化 → **上限被抹掉**。实证 n=1→100%、n=2→50/50 均违反 40% 上限，n≥3 正常。今日"仓位 100%"即此因（强市 position_scale=1.0 无纠偏）。
- **P1-3**：兜底只覆盖 None 不覆盖 NaN；全 NaN 输入静默返回 37 分（落入 else「超买 -5」）。另技术因子 7 天 TTL 与"逐日变化"事实矛盾，最坏 3+ 交易日错配。
- **P1-1**：`generate_market_briefing` 内部 `de = DataEngine()` **新建实例 → `_source_status` 默认全绿 → 推送简报结构性无法显示运行期熔断**（完整报告用 `recommendations[0]['data_source_status']` 快照，故有告警）。这是"降级结果无警示"的机制性原因。
- **P2-1**：直连同花顺 getharden 实测仅返回 6 字段（id/name/code/reason/date/market），旧映射表期待的 zhangfu 等已消失 → 题材热度 TOP10 全部 +0.0%（10/10），`row.get('涨幅%',0)` 静默兜底 0。仅展示层受影响（评分走东财 change_pct）。
- **P2-3**：配额告警文案写"100次/天"，实际闸门为 `budget-10=90`（quota.json used=90 吻合）；`predictions.created_at` 由 SQLite `CURRENT_TIMESTAMP` 写入 = **UTC**，与北京时间日志混用（今日 07:02 = 15:02）。
- **核实非缺陷（勿重复排查）**：涨跌家数口径正确（codes_cache 5218 = 沪2318+深2900，北交所前缀 0 只；3888+1132+198 自洽）；北向"市场级估算 vs 因子口径"是两源非矛盾；优化器建议不生效属设计；漂移 INSUFFICIENT(36<40) 预期。

回归验证：`python -m unittest discover -s tests -p "test_*.py"` → **91 passed / 0 failed**；真实坏缓存重放 47.0（修复前 TypeError）；正常记录 65.0 无变化；predictions 今日 1 行（3 次运行无重复落库）。

技能更新：`~/.workbuddy/skills/quant-system-full-validation/SKILL.md` 追加「当日异常根因审计（五步法）」+ 10 条复用坑位清单。

## 审计问题逐项修复（用户要求，7 项全落地）

交付：`docs/修复执行记录_2026-09-16.md`（含每项改动点、验证证据、权衡说明、遗留建议）。

改动文件（9 个）：
- `core/technical_scorer.py` — `_num` 改 `math.isfinite`（NaN/±Inf 一并按缺省兜底）
- `core/data_engine.py` — ①技术因子 live 出口统一 `sanitize_nan`（与缓存读形态等价）②`get_ths_hot_stocks` 字段漂移分级检测 ③`extract_hot_themes` 缺失涨幅→None ④配额文案改"本地安全闸门" ⑤新增 `_is_shell_tech_record` + 预取写入拒收壳记录 ⑥`import math`
- `core/portfolio_optimizer.py` — 删除归一化、改现金口径、`_equal_weight` 受上限约束、出口不变量断言、下限超额回收
- `reports/market_briefing.py` — `source_status` 入参 + 首屏降级警示 + 受影响权重合计 + 题材去伪 0.0% + 仓位行显示现金
- `strategies/short_term.py` — `_apply_position_scale` 同步 `cash_pct`
- `feedback/tracker.py` — `created_at` 显式写北京时间
- `scripts/eod_stock_picker.py` — 透传运行期数据源快照给简报
- `scripts/daily_job.py`（重写）— 运行日志落盘 `data/logs/daily_job_YYYYMMDD.log`（30 天保留）+ 配额预检 + 子进程绝不重复执行
- `scripts/prefetch_asharehub.py` — 壳记录 `reject` 计数 + 配额文案同口径

修复中当场发现的次生缺陷（归一化掩盖）：`MIN_ALLOCATION` 下限在 n=10 分数接近时把合计顶到 102.8%；现按"上限>合计100%>下限"优先级回收超额，n≥11 退化为等分。

新增 8 个回归测试文件（86 用例）：null_factor_roundtrip / briefing_degradation / allocation_cap / portfolio_sim_scale_invariance / ths_schema_drift / daily_job_logging / quota_message_timezone / prefetch_schema_guard。

验证：编译 ✅ / 单元测试 **177 passed**（基线 91）/ 门禁 **9 通过 0 失败** / 端到端实跑退出码 0 / 回测实跑退出码 0 / 组合模拟尺度不变性已证（净值不受仓位口径变更影响）。

端到端实跑证据（真实降级场景）：简报首屏出现 5 个不可用源 + "受影响因子权重合计约 81%" + 谨慎参考；题材热度不再出现 `+0.0%`，改展示代表个股；仓位显示"40%（另留现金 20%）"；落库仍 1 行（幂等生效）。验证产物另存 `data/reports/_verify_20260916_briefing_after_fix.md`，当日归档已恢复为 15:02 原始产出。

刻意偏离审计建议的两处（已记录理由）：
1. 配额预检**只告警不阻断** —— 阻断会导致当日无新简报，自动化"取最近一份"会推送昨日简报，比有降级标注的结果更危险。
2. `_run` 在日志写失败时**不重跑子进程**（避免重复烧配额）。

遗留（未执行，待用户决策）：`_simulate_portfolio` 的 `close_t0` 分支第 1119 行 `cash * allocation_pct` 未除 100（当前为死代码，默认 open_t1）；存量 23 条壳记录与 UTC `created_at` 未回填。

## 23:01 T+1 回填（自动化）

结果：成功 0 / 失败 0 / 滞留 8。微信直推 ret=-2 prepare failed（会话未激活），仅小程序送达。

- pending 16→17，增量 = 今日新推荐 600163（t1 未到期）。17 条 pending 全部被本次运行处理（updated_at 刷新至 09-16 15:01 UTC），attempts 无递增、无 t1 逾期缺失 → 无真失败。
- N=0 为**正常到期空档**，非静默失败：08-20 批仅第 19 个交易日（T+20 于 09-17 到期），08-21 批 18 个，09-07 批 7 个。
- K=8（08-20×6、08-21×2）与 09-15 同集合，无新增滞留。
- **上游恢复**：predictions 出现 09-16 批次 1 条（600163），结束 09-08~09-15 连续 6 个交易日停更；此前"疑似静默失败"判定撤销。
- 预测：09-17 回填 N 应回升至 6（08-20 批 T+20 到期出池）。
