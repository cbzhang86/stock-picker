
## 2026-09-12 自动化调度（非交易日）
- 非交易日分流 → ASHareHub 配额预取成功（30 只，配额 90/100，miss 1）。
- **修复 `scripts/daily_job.py` bug**：`_run()` 的 `extra_args` 无默认值，预取分支 3 处调用缺参导致 TypeError 必崩（此前周末/节假日分支从未真正运行成功）。最小修复：默认参数 `None` + `or []`。
- 非交易日成功不推送微信（按规则）。
