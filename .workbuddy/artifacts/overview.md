# 任务概览：代码体检修复 + 交易日尾盘定时选股推送微信

日期：2026-09-05 ｜ 项目：stock-picker-v2

## 一、代码体检（全部通过）

- `scripts/evaluate_all.py --full`：**10/10 通过**（编译、6 项回归、权重完整性、OOS 面板 hot_theme IC=+0.0580）
- 冒烟测试 `eod_stock_picker.py --mode short`：**7 分 11 秒全流程跑通**（全市场 5213 只 → 弱市评估 52.5 分 → 详评 170 只 → 简报生成）。当日无评分达标标的属正常弱市风控，未强行推荐。
- 测试产生的周六噪声数据已用备份恢复（predictions / factor_daily / asharehub_prefetch 三个 DB）。

## 二、修复的问题（3 项，均已验证）

| 问题 | 影响 | 修复 |
|---|---|---|
| `strategies/short_term.py` 缺 `import os` | 连亏降仓风控**静默失效**（冒烟日志抓到 `name 'os' is not defined`） | 补 import，单测验证通过 |
| `eod_stock_picker.py --out` 死参数 | docstring 承诺但未实现 | 已实现另存简报功能 |
| 非交互 shell 不加载 `~/.bashrc` | `ASHAREHUB_API_KEY` 进程内为空 → 数据源 401 降级 | key 验证有效（SDK 复测成功）后写入 Windows 用户环境变量；提示词内置 export 兜底 |

## 三、微信推送方案（WorkBuddy 原生，零第三方密钥）

1. **推送到小程序**：自动化任务自带开关，任务完成自动推送到手机微信里的 WorkBuddy 小程序。
2. **微信助理集成**（可选，扫码绑定）：微信聊天窗口直接收任务完成通知，也可反向在微信里下指令。

## 四、定时任务（需用户 2 步完成）

`automation_update` 工具本会话不可用（已逐项确认），最终创建在 WorkBuddy「自动化」界面完成：

1. 打开 `docs/自动化任务_尾盘选股推送.md`，按配置表创建任务（周一~五 14:50、工作空间指向本项目、**权限模式选"完全访问"**、提示词直接复制文档第三节、开启"推送到小程序"）→ 用「测试运行」验证。
2. 重启 WorkBuddy（使新的用户级环境变量生效）+ 可选完成微信助理扫码绑定。

## 五、说明与遗留

- 定时规则无法排除 A 股法定节假日；非交易日运行会如实报告无数据，脚本有当日防重，不会重复落库。
- 弱市/极差市跳过推荐是既定风控，推送中会说明原因而非静默无消息。
- 遗留（沿用）：回测/实盘评估环境不均等问题、动量过滤策略级回测确认，均待后续处理，与本任务无关。
